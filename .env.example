# Production site URL — set this in Vercel (Project → Settings → Environment Variables).
# Controls canonical URLs, the sitemap, robots.txt Sitemap line, and Open Graph URLs.
NUXT_SITE_URL=https://the-mea.com
